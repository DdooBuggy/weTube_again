mixin video(video)
    div.video-mixin
        a(href=`/videos/${video.id}`)
            if isHeroku
                div.video-mixin__thumb(style=`background-image:url(${video.thumbnailUrl});`)
            else
                div.video-mixin__thumb(style=`background-image:url(/${video.thumbnailUrl});`)
        div.video-mixin__data
            a(href=`/users/${video.owner._id}`).video-mixin__data--avatar
                if video.owner.avatarUrl === "" || !video.owner.avatarUrl
                    span 😀
                else if isHeroku || video.owner.avatarUrl.includes("github")
                    img(src=video.owner.avatarUrl)
                else 
                    img(src="/" + video.owner.avatarUrl)
            div.video-mixin__data--text
                span.video-mixin__title=video.title
                div.video-mixin__meta 
                    span #{video.owner.name} • 
                    span #{video.meta.views} 회 
